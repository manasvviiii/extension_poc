services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: warmgraph
      POSTGRES_USER: warmgraph
      POSTGRES_PASSWORD: warmgraph_dev
    ports:
      - "5432:5432"
    volumes:
      - warm_graph_postgres:/var/lib/postgresql/data

volumes:
  warm_graph_postgres: